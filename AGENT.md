# AGENTS.md

本文件是 Codex 和其他 AI 编码助手的项目入口；`CLAUDE.md` 中除 Claude Code 专有表述外，均视为本项目通用约束。

## 必读顺序

1. 先读 `MEMORY.md`。
2. 再读 `CLAUDE.md`。
3. 涉及具体主题时，继续读取 `docs/memory/`、`docs/requirements/` 或 `openspec/` 下对应文件。

## 稳定规则

1. 全程使用简体中文；代码、链接、命令、协议和技术术语保留原文。
2. 以当前仓库事实为准，不得直接套用 `/server/ai-agent` 或其他项目的历史状态。
3. 新增功能、行为修改、协议改造或测试规则变化时，优先遵循 `CLAUDE.md` 中的 OpenSpec 工作流。
4. 未经用户明确确认，不得执行 `git add`、`git commit`、`git push`。
