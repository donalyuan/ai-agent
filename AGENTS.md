# AGENTS.md

本文件是 Codex 和其他 AI 编码助手的项目入口；`CLAUDE.md` 中除 Claude Code 专有表述外，均视为本项目通用约束。

## 必读顺序

1. 先读 [项目事实](docs/agent/PROJECT.md)、[长期决策](docs/agent/DECISIONS.md) 和 [当前交接](docs/agent/HANDOFF.md)。
2. 再读 `CLAUDE.md`。
3. 涉及具体主题时，读取 `docs/requirements/` 或 `openspec/` 下的对应文件；诊断问题时读取 [排障记录](docs/agent/TROUBLESHOOTING.md)。

## 事实优先级

1. 当前代码、测试、schema 和可执行配置优先于所有记忆文档。
2. ADR 与当前架构文档其次。
3. [项目事实](docs/agent/PROJECT.md) 与 [长期决策](docs/agent/DECISIONS.md) 再次。
4. [当前交接](docs/agent/HANDOFF.md) 与 [排障记录](docs/agent/TROUBLESHOOTING.md) 最后。

文档与高优先级事实冲突时，以高优先级事实为准并修正受影响的记忆记录。

## 记忆维护

1. 任务完成后，更新受影响且已确认的项目事实、长期决策、当前交接或可复现排障记录。
2. 只保存后续任务可复用的确认信息；不保存临时探索、一次性报错、未验证猜测或短暂的局部步骤。
3. 保持文档精简并优先使用相对链接指向权威来源，避免在多处复制同一规则。
4. 不得将口令、密钥、API key、令牌、凭据、私有数据或设备相关绝对路径写入项目文档。

## 稳定规则

1. 全程使用简体中文；代码、链接、命令、协议和技术术语保留原文。
2. 以当前仓库事实为准，不得直接套用其他项目的历史状态。
3. 新增功能、行为修改、协议改造或测试规则变化时，优先遵循 `CLAUDE.md` 中的 OpenSpec 工作流。
4. 未经用户明确确认，不得执行 `git add`、`git commit`、`git push`。
5. 需要执行 `git commit` 时，提交备注必须使用简体中文，不得使用英文提交信息。
6. 涉及前端 UI、交互或视觉重构时，必须先制作可运行的原型供用户确认；确认前只允许使用静态演示数据，不得接入真实 API、数据库或修改正式业务行为。收到明确确认后，才能进入正式开发与业务联调。
7. 前端原型与正式页面必须使用用户确认的组件库；不得自行选择、替换或假定组件库。组件库未在仓库事实或用户指令中明确时，必须先确认再开发。
8. 前端页面不得新增页面级手写 CSS；布局、间距、颜色、排版和交互状态必须由已确认组件库及其主题/token 提供。若现有组件库不支持所需能力，先补充组件库能力并获得确认。

## 能力路由

1. 当前会话可用 `frontend-design` 时，新建或重构 UI 必须使用该 skill。
2. 当前会话可用 `playwright` 时，需要浏览器交互或端到端检查必须使用该 skill。
3. 执行当前项目前端开发、调试或验证任务时，可自动截取当前项目的浏览器页面或应用窗口；浏览器页面优先使用 `playwright` 的工具内截图，代理自查截图仅保存于临时目录。
4. 整个桌面、其他应用、其他项目或无关内容的截图，即使在前端任务中也不自动允许；非前端任务的任何截图及其他范围外截图，均须事前向用户索要明确授权。
5. skill 的当前可用性不是跨设备的仓库事实；能力缺失时必须说明原始限制，不得伪装为已执行。
