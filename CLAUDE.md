# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 语言与沟通

1. 全程使用简体中文。
2. 结论优先，证据其次，非必要不展开。
3. 代码、命令、路径、协议、技术术语保持原文。
4. 同一轮回复中，不重复输出语义相同的方案、结论或问题；若需修正，直接基于上一次内容增量说明。

## 项目记忆

1. 项目记忆采用 `docs/agent/` 下的分类 Markdown：`PROJECT.md` 记录当前事实，`DECISIONS.md` 索引长期决策，`HANDOFF.md` 记录当前可继续执行状态，`TROUBLESHOOTING.md` 只记录已证实问题，`README.md` 提供入口。
2. 每次新会话开始前、每次上下文压缩后恢复继续执行前，必须先读取 `docs/agent/PROJECT.md`、`docs/agent/DECISIONS.md` 和 `docs/agent/HANDOFF.md`；诊断问题时再读 `docs/agent/TROUBLESHOOTING.md`。
3. 只保存已确认并在后续仍可能复用的关键信息，包括长期偏好、稳定规则、历史决策，以及跨轮或压缩恢复后继续执行仍必需的上下文。
4. 需求明确或架构决策一旦确认，必须在当轮同步更新对应的记忆文件；若为长期决策，更新 ADR 与 `DECISIONS.md` 索引。
5. 禁止写入临时探索、一次性报错、未确认猜测、仅当前局部步骤短暂有效的信息，以及口令、密钥、令牌、隐私数据等敏感信息。

## 当前仓库状态

1. 易变化的项目状态、已验证技术栈和当前交接以 `docs/agent/PROJECT.md`、`docs/agent/DECISIONS.md` 与 `docs/agent/HANDOFF.md` 为准，本文件不复制这些快照。
2. 后续实例在进入实现前必须重新检查仓库实际内容、当前 OpenSpec change 和上述项目记忆，以真实文件为准，不能从历史对话或其他项目推断当前状态。

## 运行环境

1. 运行和验证入口以根 `package.json`、`README.md`、锁文件、Compose 配置及 `docs/agent/HANDOFF.md` 的当前证据为准。
2. 不得把尚未实际验证的命令、运行时或外部服务写成项目既定事实；未经明确确认，不得在宿主机安装与项目运行环境不一致的替代运行时。

## 测试与验证

1. 默认采用 TDD：先写测试，再写实现。
2. 声称完成前，必须运行直接相关测试或验证命令，并以实际输出为准。
3. 证据优先级：
   - 运行报错
   - 代码事实
   - 文档约定
4. 未验证的实现不得声称“已完成”。

## 能力路由

1. 当前会话可用 `frontend-design` 时，新建或重构 UI 必须使用该 skill。
2. 当前会话可用 `playwright` 时，需要浏览器交互或端到端检查必须使用该 skill。
3. 执行当前项目前端开发、调试或验证任务时，可自动截取当前项目的浏览器页面或应用窗口；浏览器页面优先使用 `playwright` 的工具内截图，代理自查截图仅保存于临时目录。
4. 整个桌面、其他应用、其他项目或无关内容的截图，即使在前端任务中也不自动允许；非前端任务的任何截图及其他范围外截图，均须事前向用户索要明确授权。
5. skill 的当前可用性不是跨设备的仓库事实；能力缺失时必须说明原始限制，不得伪装为已执行。

## 代码可读性

1. 关键变量、关键方法、关键流程节点必须补充简洁备注，说明其业务意图、输入输出或存在原因。
2. 备注应解释“为什么这样设计”或“这个值或方法承担什么职责”，不得用无信息量的注释重复代码表面含义。
3. 复杂流程、外部 API 对接或非直观约束必须有清晰的文档字符串或维护说明。

## 风险控制

1. 若用户要求与协议、现有约束或代码事实冲突，必须按“结论｜证据｜可执行替代方案”明确指出。
2. 不接受临时兜底方案冒充彻底解决方案。
3. 未经用户明确确认，不得擅自执行 `git add`、`git commit`、`git push`，或将文件提交到仓库。
4. 需要执行 `git commit` 时，提交备注必须使用简体中文，不得使用英文提交信息。

## 架构判断

1. 已接受架构和当前实现边界分别以 `docs/adr/`、权威技术文档、当前代码与可执行配置为准；目标架构不得被误写为已经完成的实现。
2. 新增应用、服务或共享组件的落点需要在对应需求和架构决策确认后确定，不得从历史对话或已删除结构推断。

## OpenSpec 规范

1. 本工作区涉及功能新增、行为修改、协议改造、测试规则变化时，必须优先遵循 OpenSpec 工作流。
2. 开始实现前，必须先检查 OpenSpec 当前状态：
   - `openspec list --json`
   - 若已有相关 change，优先在已有 change 上继续
   - 若没有相关 change，先新建 change
3. 用户提出需求后，默认先进入 OpenSpec 的需求澄清或设计阶段，不得跳过设计直接改代码。
4. 需求讨论阶段，内部必须先从 `DDD`、`BDD`、`SDD`、`TDD` 四个角度完成审视，再对外输出结论。
5. 需求讨论阶段对外回复时，必须显式按 `DDD`、`BDD`、`SDD`、`TDD` 四个标签组织内容；若某一角度当前无新增约束或影响，也必须明确写出，不得省略。
6. 四个角度的最小审视口径如下：
   - `DDD`：领域概念、边界、状态流转、规则归属。
   - `BDD`：用户场景、触发行为、可观察结果、验收口径。
   - `SDD`：规格、接口、数据结构、约束、兼容性、非目标。
   - `TDD`：测试入口、失败场景、回归范围、验证方式。
7. 需求边界不清时，必须先澄清；证据不足时，不得主观猜测后直接实现。
8. 确认方案后，必须在 `openspec/changes/<change-name>/` 下补齐当前 schema 需要的 artifacts，至少包括：
   - `proposal.md`
   - `design.md`
   - `specs/**/*.md`
   - `tasks.md`
9. 实施过程中，代码改动必须与对应 OpenSpec tasks 对齐；完成一项就同步勾选 `tasks.md`。
10. 实现完成后，必须再次执行：
    - `openspec instructions apply --change "<change-name>" --json`
    - 确认 `state` 为 `all_done` 或任务进度与实际一致
11. 若实现过程中发现设计或范围变化，必须先回写对应 OpenSpec artifacts，再继续编码。
12. 未经明确确认，不得绕过 OpenSpec 直接做“先改代码后补文档”。

## 工作约束

1. 当用户只要求生成仓库约束文件时，只创建或修改用户明确请求的规则文件，不要顺带创建脚手架、示例代码、依赖清单或测试文件，除非用户明确要求。
2. 禁止脱离已确认需求和 OpenSpec 凭空补应用代码、未验证配置或示例 Schema。
3. 以后若仓库新增了 README、其他 AI 工具规则文件，更新 `CLAUDE.md` 时只提炼其中长期有效、会影响 Claude Code 行为的部分，避免重复抄录。
4. 把命令写进 `CLAUDE.md` 之前，必须先在本仓库实际运行并确认可用；没有验证过的命令不要写成既定事实。
