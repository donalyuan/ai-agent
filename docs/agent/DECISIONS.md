# 长期决策

| 状态 | 决策 | 说明 |
| --- | --- | --- |
| 已接受 | [ADR-0001：采用 Git 与 Markdown 项目记忆](../adr/0001-use-git-markdown-project-memory.md) | 第一阶段以版本控制文档维护项目记忆，不引入服务。 |
| 已接受 | [ADR-0002：阶段 0 工程边界与本地运行形态](../adr/0002-phase-zero-foundation-boundaries.md) | 单仓库、共享 Schema、Mock/Local 默认与 Compose 本地运行基线。 |
| 已接受 | [ADR-0003：后端采用模块化单体与分层端口架构](../adr/0003-backend-modular-layered-architecture.md) | 目标结构按业务模块分层，Command/UoW/Outbox 与 Temporal/Worker 副作用边界固定；阶段 0 代码按功能切片迁移。 |
| 已接受 | [ADR-0004：分离 Skill 来源与保护长期审计事实](../adr/0004-separate-skill-provenance-and-protect-audit-facts.md) | AgentScope runtime 独立管理；Git/公开 Markdown Skill 使用不同 source identity；关键运行、审核、能力、调用摘要和被引用素材长期 no-GC。 |
| 已接受 | [ADR-0005：对齐 MVP-A 资产中心与能力边界](../adr/0005-align-mvp-a-asset-center-and-scope-boundaries.md) | 新增第五个前端/业务闭环及 Project/Episode creative、AssetBible continuity owner；冻结结构化文本非小说正文、完整 AssetVersion 编辑、通用 reservation/upload/register、人工业务审核、successor Run、人工 Skill 裁决、重拍替换、逐集批量导出及资源/codec 预检边界。 |
| 已接受 | [ADR-0006：闭合阶段一运行诊断与媒体交接](../adr/0006-close-phase-one-runtime-diagnostics-and-media-handoffs.md) | 阶段一扩为 1 overall + 18 child；补齐 historical snapshot 新 Run、Episode 状态隔离、SoundCue、导出定位/上传登记、实际 2 GiB、local observability 和 localhost-only/LAN MVP-B 边界。 |
| 已接受 | [ADR-0007：以关系表持久化 AssetBible owner 事实](../adr/0007-persist-asset-bible-as-relational-owner-facts.md) | AssetBible 的 entry/version、override、snapshot、impact/accept/task/handoff 使用关系表、行级 CAS 与项目归属约束；旧 document 只迁入一次，consumer 只读冻结引用。 |
| 已接受 | [ADR-0008：规范化 Catalog 与图片候选交接边界](../adr/0008-catalog-normalized-and-image-candidate-boundaries.md) | Catalog owner facts 使用规范化关系表与 CAS/append-only 语义；GPT Image 先校验 accepted AssetBible snapshot，再登记未引用 immutable candidate，Mock/Local 默认且真实 transport 缺失保持 unconfigured。 |
| 已接受 | [ADR-0009：Agnes 异步视频操作与派生物边界](../adr/0009-agnes-video-operation-and-derivative-boundaries.md) | Agnes operation/candidate 使用 `run_id + logical_operation` 幂等与规范化 owner 表；Provider、Scenes、Media Worker、Timeline 各自拥有 transport/current/derivative 事实。 |
| 已接受 | [ADR-0010：Agent AssetEdit 审查与版本交接边界](../adr/0010-agent-asset-edit-review-boundaries.md) | AssetEdit 只拥有 image/video intent、对话、候选和审查；完整 AssetVersion、accepted continuity snapshot、exact CAS 和 canonical schema_version 是强前置，Provider/Timeline/AssetVersion owner 边界不泄漏。 |
| 已接受 | [ADR-0011：分离存储会话与素材版本登记](../adr/0011-separate-storage-sessions-and-asset-registration.md) | Storage 只拥有 profile/bucket/upload session/object/proof/recovery；Assets reservation 以稳定 operation key 发起，verified object 再由 Assets owner 幂等登记；Local 支持磁盘 multipart 重启恢复，真实 TOS 前置缺失保持 unconfigured。 |
| 已接受 | [ADR-0012：分离 Timeline、Media 与导出产物 owner](../adr/0012-separate-timeline-media-and-export-artifact-owners.md) | Episode 拥有唯一 current Cut 与 immutable TimelineVersion；Media Worker 拥有 inspection/derivative/preview；canonical RenderPlan 驱动逐集 MP4/SRT/light，artifact 下载只返回短 TTL opaque read grant，真实 renderer 缺失保持 unconfigured。 |
| 已接受 | [ADR-0013：阶段一 owner UI 与未配置外部能力边界](../adr/0013-phase-one-owner-ui-and-unconfigured-boundaries.md) | 阶段一 UI 只消费 owner projection，Timeline/Export/Settings command 绑定 scope 与 CAS；真实 Provider/TOS/FFmpeg 缺失保持 unconfigured/503，Mock + Local offline 继续可验证。 |
| 已确认（OpenSpec） | [阶段 1 MVP-A 的 20-change 交付契约](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) | 1 个总体 change 加 19 个 child；五个前端/业务 change 负责用户闭环。projects/episodes 分别拥有项目级与每集 creative current，AssetBible 拥有 typed entry/version、四层 override、accepted resolved snapshot、impact 与 revision task。文本 owner 仅追加 accepted handoff，Project/Episode/Scene/Shot/AssetBible 各 owner必须以 typed command/ack 落地，全部 ack 前暂停付费媒体。failed Run successor 与 historical snapshot rerun 分离，Skill 歧义由用户裁决，重拍通过 `ReplaceClipSource` 更新 current Cut 后另行发布 TimelineVersion，项目导出由 `EpisodeExportBatch` 逐集生成并登记 artifacts；Provider 优先级与冻结、无隐式 fallback 规则不变。 |
| 已确认（OpenSpec） | [阶段 1 媒体、版本与导出冻结规则](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) | StorySpec 项目级、ScriptSpec 每集级、Scene/Shot 稳定 ID、ShotSpec 版本化，AssetBible 覆盖为 project -> episode -> scene -> shot。付费 operation 幂等，`submission_unknown` 先 reconciliation，取消晚到结果为未引用候选。产品需求中 MVP-A/MVP-B 对完整工程包的冲突统一为：MVP-A 仅交付 `exportProfile=light` 的 manifest/可解析引用且不回导，MVP-B 才交付 `portable` 媒体载荷；timeline 固定 9:16/16:9/1:1、1080p、30fps、H.264 `yuv420p`、AAC 48 kHz、UTF-8 SRT。 |
| 已确认（OpenSpec） | [阶段 1 费用、供应链与外部输入安全](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) | catalog/security owner 负责通用 Provider Credential 的 AES-256-GCM 与 Docker Secret 主密钥；缺主密钥时真实 Provider 返回 503，Mock/Local 继续可用。catalog/workflows/settings UI 共同遵守项目文本阈值、图片/视频批量确认、`cost=unknown` 强确认和精确 `run_id + logical_operation` 绑定；诊断至少保留 30 天，长期审计记录 `retention_policy/version/hold` 与稳定本地 `user_uuid`。AgentScope runtime 与 Skill provenance 分开管理；Git Skill 使用 commit/digest，公开 Markdown Skill 使用 archive URL/获取时间/digest/license status，路由后才读取正文。`RunEvent`、`AcceptDecision`、`CapabilitySnapshot`、脱敏 `ProviderCall` 摘要和被引用 `AssetVersion` 长期 no-GC。GPT Image 与 Agnes 安全边界保持不变。 |
| 已确认（OpenSpec） | [阶段 1 六类精确门](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) | Image 必须经 accepted provenance/candidate/version hash-revision 的精确 CAS 后才成为 current；Text 上游编辑产生 successor/stale closure，批次 CAS 全有或全无；catalog entity 以 expectedRevision/If-Match 变更，SkillRevision 只追加；credential 固定 AES-256-GCM envelope 与版本化 keyring；MP4/SRT/light 为独立 ExportArtifact 且安全短授权下载；MVP-A 转场仅 cut/crossfade、30fps 整数帧并与 FFmpeg 共用 canonical RenderPlan。 |
| 已确认（OpenSpec） | [阶段 1 范围冲突统一](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) | MVP-A 只消费固定、版本化、已发布 `drama-mvp-a-default` WorkflowVersion；ensure 仅由用户显式生成 command 触发，页面加载/视图切换零 workflow mutation；工作流图编辑、连线、草稿保存和发布 UI 延后 MVP-B。SourceMaterial 固定为 `creationMode=original|adaptation`、`materialType=novel|synopsis|existing_script`、`inputMode=inline_text|uploaded_file` 的 import/parse/validation/binding/recovery contract。Agent 仅可执行 image/video `AssetEditPlan`；story/script 继续 TextReview successor/stale closure，audio/Timeline 继续 Timeline editor typed commands。对白 ducking 的 enabled、对白区间、衰减量、attack/release、目标轨道冻结到 TimelineVersion，并由 canonical RenderPlan/FFmpeg 映射。 |
| 已确认（OpenSpec） | [阶段 1 媒体执行与审核 owner](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) | AssetEdit 拥有 `executeAssetEditPlan` intent/status/reconcile 与 Candidate review；GPT Image/Agnes 只拥有 Provider transport/result handoff；Scenes 以 exact candidate/provenance/version CAS 写 current；Workflow 拆分 media generate/review/inspect/handoff；Media Worker 生成 canonical metadata、proxy、thumbnail、keyframe index、waveform；Timeline 不接受未审核或 derivative 未 ready 的媒体。 |
| 已确认（OpenSpec） | [阶段 1 外部 adapter 与 StorageProfile 边界](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) | Codex relay 是显式启用真实文本能力后的推荐 default live Profile，DeepSeek opt-in；首次运行、CI 和默认本地测试仍为 Mock/Local。真实 adapter 明确 `/v1/models` candidate diff、认证、重试、解析和 probe；StorageProfile/Bucket/Region/Endpoint、连接测试、启停与 credential status 属 MVP-A，真实 TOS 仅显式 profile/probe，不能隐式回退 Local。 |

新增长期决策时，先创建或更新 ADR，再在此添加简短索引。当前架构与可执行实现优先于本索引。
