# 项目事实

## 当前状态

- 记录日期：2026-08-24（阶段一 review 修复复核）。
- **已实现：阶段 0**：单仓库工程基线已完成；包含 React/Vite 工作台壳层、FastAPI 健康接口、PostgreSQL/Alembic 基础模型、共享 JSON Schema、Mock Provider、LocalWorkspaceAdapter、SkillRegistry/SkillRouter、Temporal 与三类 Worker 健康入口。
- **已实现：阶段 0 垂直切片**：`projects/episodes` 已完成 domain/application/Repository/UoW/SQLAlchemy migration/HTTP API 及架构、契约和 BDD/TDD 测试；业务 API 已在 Compose PostgreSQL 上验证。
- **已实现：Assets/AssetVersions 垂直切片**：素材身份与只追加版本已完成 domain/application/in-memory/SQLAlchemy/HTTP、Alembic `0004` 安全回填、`0005` 完整性 repair 与 `0006` legacy workspace reference repair（revision ID 为 25 字符的 `0006_assets_legacy_repair`）、架构与 BDD/TDD 测试；`repair-assets-object-key-contract` 以 `packages/contracts` 的机器可读 corpus 统一 Schema/domain/HTTP/`0004`/`0006` 的 canonical relative objectKey，拒绝尾随空段、Python `strip()` whitespace（含 newline/U+0085）、绝对/UNC/drive-qualified/drive-relative、无 `//` 的 RFC scheme、`?`/`#` 分隔符、反斜杠和点段。legacy migration 只解析完整 `workspace://` URI；`0004`/`0006` 均在 DDL 前以各自冻结的 Python helper 预检，并参数化持久化 helper 返回的 canonical 值，不以 SQL `trim()`/`substr()` 重解析原始值；`0006` 不改写普通 legacy provider/bucket。`AssetVersion`、`StorageObject` 和嵌套 media 深度不可变，数据库强制 AssetVersion 项目归属组合关系和 64 位十六进制 hash，只保存 storage object reference 和元数据，不保存媒体二进制。
- **已定义：接口/目标架构**：`define-backend-engineering-architecture` 与 ADR-0003 固定模块化单体、Ports/Adapters、`interfaces -> application -> domain` 依赖方向，以及后续 Outbox/Temporal/Worker 边界；目标架构不等同于全部已实现代码。
- **待实现：产品能力**：完整真实媒体生成链路、专业剪辑、协作、移动端、平台发布、真实 Provider/TOS 与 SSE。AgentScope 文本和 MVP-A Timeline/Media/export 切片已实现，但真实文本 Provider 与 FFmpeg 都需显式配置/probe，不能据此宣称 live 生成或渲染成功。
- OpenSpec 状态：阶段 1 当前有 20 个未归档且已完成的 changes，即 1 个总体协调 change 和 19 个 child；正式 UI 迁移 change `redesign-phase-one-workbench-ui` 已同步主规格并归档。`prototype-shadcn-workbench` 不存在于当前 artifacts，且不得恢复或创建。495 项 tasks 已与历史实现和本轮 UI/集成验证同步为完成；真实 Provider/TOS/账号/FFmpeg 仍缺失时保持 `unconfigured`、`renderer_unconfigured` 或 503，默认验证路径为 Mock Provider + Local offline（`local_workspace`）。
- **已完成：阶段一正式 UI 迁移**：`redesign-phase-one-workbench-ui` 已完成 20/20 并归档。`/prototype` 仍是零网络静态确认页；正式 Workbench、Review、Assets、Timeline、Exports、Provider/Storage Settings 已收敛到共享壳层、`shared/ui` 与 Tailwind token，未新增页面级 CSS。`/projects` 保留全局导航并按自然内容高度布局；带项目 ID 的深链仍使用全屏工作台壳层，滚动画布在宽屏使用完整可用宽度，当前剧集选择并入标题上下文区。Review 与 AssetEdit 的 project-scoped ID-only 请求显式携带 `X-Project-Scope`，仍按 owner revision/CAS 和 409 权威重读工作。
- 阶段一退出报告已落盘为 `docs/evidence/E2E-MVPA-001.json`，包含 `S01`-`S11` 及 `S03a`、`S04a`、`S08a`、`S08b`、`S11a` 共 16 行 owner/prerequisite/success/failure/no-side-effect 证据；Review 专项证据为 `docs/evidence/E2E-MVPA-001-context-agent-review.json`。
- **已实现：阶段一 Storage owner 与 Local 大对象链路**：Alembic `0019_storage_owner` 增加 StorageProfile/private bucket、upload operation/session/part、StoredObject/reference proof/recovery 规范化表；`LocalWorkspaceAdapter` 使用磁盘 session manifest 支持 multipart 重启恢复、完整性 gate、短期访问、受证明删除与 24h/7d cleaner。Assets reservation 的 operation key 精确包含 reservation ID，verified object 只能由 Assets owner 幂等登记；SourceMaterial、ExportArtifact 和 audio 维持独立 owner handoff。有效 PNG actual-byte 证据已通过 2 GiB、32 分片、重启恢复、单一 AssetVersion、inspection/proxy；真实 TOS SDK/account/private bucket/Docker Secret/credential 仍缺失并明确 `unconfigured`，无 live request 或 Local fallback。
- **已实现：阶段一 Timeline/Media/export owner**：Alembic `0020_timeline_export_owner` 增加每集唯一 current Cut、Clip/SoundCue/字幕、immutable TimelineVersion、MediaInspection/Derivative/PreviewArtifact、EpisodeExportBatch/Job/Artifact/Diagnostic 关系表，`0023_export_dispatch_owner` 增加冻结执行快照和持久 export dispatch outbox；typed command 立即 revision CAS，Timeline 只消费 accepted current + exact ready derivative。Media Worker 通过持久 dispatcher 启动稳定 ID 的 Temporal workflow/activity，只从冻结 Timeline/RenderPlan/Storage/renderer/输入/provenance 快照执行；输入和 MP4/SRT/light 均有界流式读写并按冻结 capability multipart，Storage unknown 先 reconcile 且不重渲染，确定性漂移记录 owner diagnostic 并落 `failed`，可重试存储错误保留执行态。artifact 下载校验完整归属/retention/license/hold 后只返回 <=300 秒 opaque read grant。真实 `FFMPEG_PATH`/`FFPROBE_PATH` 未配置，当前明确 `renderer_unconfigured`，没有伪造 live 成功。
- **已实现：阶段一项目资产中心**：Alembic `0022_asset_center_owner` 增加 Asset catalog metadata 与规范化 AssetVersionReservation；`/projects/:projectId/assets` 提供稳定 cursor/六类过滤、metadata CAS、Local multipart create/resume/cancel/reconcile/register、Media readiness、短 TTL opaque grant、usage partial/unavailable 和 Timeline exact selection handoff。浏览器已验证同一 reservation/session 失败恢复、图片与 WAV 单版本登记、筛选/usage/Timeline 导航及桌面/移动布局；实际 2 GiB 的 32 分片、Adapter 重启恢复、checksum/ETag/MIME、单一版本、inspection/proxy 证据见 `docs/evidence/E2E-MVPA-001-asset-center-2gib.json`。真实 TOS/FFmpeg 仍为 `unconfigured`。
- **已实现：阶段一用户闭环 UI**：Workbench/Review/Timeline/Exports/Provider-Model-Skill/StorageProfile 均通过共享 project shell 真实导航；Review 按 project+Episode 恢复 owner session，candidate accept/reject/retake 使用显式确认和精确 CAS；Timeline 使用 30fps 整数帧、SoundCue/caption/ducking projection、立即 command 与 TimelineVersion compare；Exports 可组装去重的多 Episode/published TimelineVersion/output name 集合并一次提交，失败重试必须显式勾选非空 Episode 集合，只有 succeeded + verified + approved + 非 hold + 未过期 artifact 才能请求 opaque download grant；Settings credential 只读 masked/unconfigured，编辑普通字段保留既有 binding/credential reference，probe/connection-test 不隐式触发。桌面 1440px 与移动 390px 浏览器验证无横向溢出或控件越界。
- 阶段一 E2E 入口为根 `pnpm run test:e2e`，使用 `apps/web/playwright.config.ts` 在 Chrome 146 与 Edge 151 各执行一次只读共享导航；固定 300-node workflow projection 在两浏览器均确认 300 个节点元数据，节点列表为虚拟化有界 DOM，导航期间写请求为 0。localhost 4 条普通 API 各 100 样本、10 warm-up，最大 P95 为 38.859ms（阈值 500ms）；原始结果见 `output/playwright/e2e-results.json` 与 `docs/evidence/E2E-MVPA-001.json`。
- Web 单元测试由 `apps/web/vitest.config.ts` 限定为 `src/**/*.test.{ts,tsx}`，Playwright E2E 不再被 Vitest 收集；`apps/web/.prettierignore` 与 `apps/web/.gitignore` 忽略 runner 生成的 `output/` 与 `test-results/`，根 `.gitignore` 忽略 `.playwright-cli/` 日志/快照/截图；根 `output/playwright/` 则刻意保留为阶段退出证据，因此根 `pnpm run check` 的单元测试与 E2E/产物格式边界稳定分离。
- 阶段一当前复核：Contracts 78、Web 75、API 432；`test:e2e` Chrome/Edge `2/2`，20 个未归档 changes strict validation 与 `git diff --check` 均通过；Compose 七项服务 healthy，真实 renderer 仍保持显式 `renderer_unconfigured`。
- **已实现：阶段一 Project/Episode creative owner**：Project 持有 `creationMode`、不可变 CreativeBrief/设置与 StorySpec current，Episode 持有 ScriptSpec current；accepted text handoff 通过 typed command、revision CAS 和幂等 ack 落地，不复制 catalog/Run/SourceMaterial owner facts。
- **已实现：阶段一结构化文本切片**：Agent Worker 已引入 AgentScope 2.x 独立 runtime dependency；Skill Registry/Router 使用 approved immutable revisions、按来源类型记录 provenance，并在路由后惰性读取正文。文本链路只生成并逐对象校验 StorySpec/ScriptSpec/Scene/Shot/ShotSpec 与实际引用的初始 AssetBible specs，支持 successor/stale closure、一次完整 TextReviewBatch 和 owner ack 门；默认仍为 Mock Provider + 显式 Local profile（adapter identity=`local_workspace`）。
- **已实现：阶段一 Scene/Shot owner**：Scene/Shot、ShotSpec、排序 revision、handoff ack、image/video exact candidate eligibility 与 accepted AssetVersion current CAS 已落地；Scene/Shot canonical facts 使用关系表和行级 revision，不写回整体 JSON 文档。accepted AssetBible snapshot 与 pending continuity task 是媒体 accept 前置，workflow scope 查询保持只读。
- **已实现：阶段一 AssetBible continuity owner**：六类 typed entry 使用稳定 ID 与不可变 version，支持 project→episode→scene→shot override、deterministic resolved snapshot、完整 impact target set/hash、原子 accept/AcceptDecision/ContinuityRevisionTask、TextReview typed handoff ack 和 owner-safe consumer projection。Alembic `0014_asset_bible_owner` 建立 10 张 canonical owner 表与行级 CAS/FK/唯一/type/hash/project-scope constraints；旧 AssetBible document 只在关系表为空时迁入一次，成功后删除，consumer 不可直接写 owner 或自动重生成下游。
- 本轮追溯补齐八类阶段一合同：指定历史 `RunInputSnapshot` 只创建新 Run；Workbench/Review 按 `projectId + episodeId` 隔离恢复 presentation/session refs；`SoundCue.track` 统一映射 PRD `cueType` 并冻结 start/duration/trigger/priority/continuity refs，automation/keyframes 延后；ExportJob 八态内增加 packaging upload/verify/register subphase 与 `ExportDiagnosticTarget`；实际 2 GiB multipart/AssetVersion/Media Worker 链路进入退出门；新增 W3C/secret-free/low-cardinality `implement-local-observability`；MVP-A 只监听 localhost/`127.0.0.1`，LAN auth 延后 MVP-B。`E2E-MVPA-001` 使用 `S01`-`S11` 加 `S03a`、`S04a`、`S08a`、`S08b`、`S11a`。
- 已归档的 `establish-phase-zero-foundation`（35/35）、`define-backend-engineering-architecture`（7/7）、`implement-projects-episodes-slice`（19/19）、`implement-assets-asset-versions-slice`（20/20）和 `repair-assets-object-key-contract`（13/13）仍是已实现事实。Assets 的历史 `sol_max_closure` 仍有效 fail，旧 lineage 仍为 `scope_decision_required`；不得将 replacement 结果误写成旧 lineage pass。
- 阶段一 Skill 实现已区分 AgentScope runtime dependency、Registry candidate、approved SkillRevision 与 Workflow binding：AgentScope 2.x 由 Agent Worker 依赖清单与 lock 独立管理；八项 candidate 中默认 Workflow 仅绑定 approved 的 `novel-writing` 与 `drama-skills`，其余六项保持 `pending_provenance`/disabled。Worker 启动读取 Registry 索引与 approved metadata，路由后才读取选中 revision 的 `SKILL.md` 与必要 references；Git 与公开 Markdown 使用不同 source identity。catalog capability snapshot、显式 probe 和运行冻结均已由 child 实现并通过测试，缺少真实凭据时仍保持 unconfigured。
- 阶段一 review 暴露的 Run/Provider/provenance 运行缺口已关闭：Run 只能从当前持久 Skill route 与 approved/enabled catalog revision 冻结统一 upstream SHA-256 digest、Provider/Profile/Model 和各 operation capability snapshot；Agent Worker 通过持久 Temporal start ledger 启动稳定 workflow ID；Text 只在 matching running NodeRun 上消费并严格校验 Provider 返回的完整计数/编号/归属/AssetBible 引用候选图；Video submit 强制 Run/node/current catalog/confirmed BudgetGate，reconcile 只查询 `submission_unknown` 的持久 provider request；ProviderCall 保存真实 capability snapshot；所有 phase-one project scope 入口缺失 `X-Project-Scope` 时拒绝。
- 阶段一 review 的数据完整性与授权漏洞已关闭：统一 router dependency 在 owner read 前拒绝缺失/foreign `X-Project-Scope`，ID-only route 在 application service 内核对真实 owner；collection-backed `PhaseOneDocument` 使用 loaded revision CAS；Temporal text activity 实际生成完整 batch 并进入 `waiting_review`；Text ProviderCall 使用 durable claim、完整 operation binding、受控 usage/脱敏失败与 ambiguous no-resubmit；SourceMaterial binding 只接受真实 current owner facts，uploaded source 从同项目有效 AssetVersion 派生 hash 与 `parsed/valid`；Local storage 统一 canonical key、完整 multipart binding、fail-closed delete proof，并由 Compose API/Media 共享同一 `local-workspace` volume。
- Catalog bootstrap 已按关系依赖分阶段幂等持久化 Provider/Skill、Profile、Model 与 CapabilitySnapshot，避免 PostgreSQL 在同一无 ORM relationship 的 collection-backed UoW 中先 flush CapabilitySnapshot 而触发 Model FK 失败；当前 Compose PostgreSQL 已升级到 `0023_export_dispatch_owner`，七个服务已用本轮镜像重建并保持 healthy。
- 阶段一长期保留合同已增加跨 change no-GC 验收，并同步 workflows/runs、AssetEdit、catalog、AgentScope 与 operations resilience 的 owner/清理边界：`RunEvent`、`AcceptDecision`、`CapabilitySnapshot`、脱敏 `ProviderCall` 摘要和仍被引用的 `AssetVersion` 在诊断窗口过期、Worker temporary/derivative cleanup、容量维护、恢复和 GC 后仍须可读取、append-only 且不得被删除、覆盖或静默压缩；只有明确无引用且符合 retention policy 的临时对象可清理，相关测试已通过。
- 阶段 1 OpenSpec 的五类跨 change 合同均已实现并验证：Text successor/stale closure/batch CAS；AES-256-GCM credential envelope/keyring rotation；独立 ExportArtifact 安全下载；`cut|crossfade` 30fps canonical RenderPlan parity；以及 `E2E-MVPA-001` 退出报告。image candidate accepted provenance/精确 CAS、Agnes preflight/异步 lifecycle 和 Provider/Media derivative ownership 已在运行时代码与 `0017_agnes_video_owner` 落地。
- Assets 旧 closure 的产品 blocker 已由独立 `repair-assets-object-key-contract` 覆盖；旧 controller 的 claims、repair records 与 max charter 仍不足以关闭旧 lineage。用户授权的 claims 超集 replacement lineage `complete-assets-asset-versions-slice-v3` 已完成全量验证，独立 `sol_xhigh` 有效 pass，controller `close_check` 返回 `close_allowed=true`；该 OpenSpec change 已通过 replacement lineage 完成收口并已归档。
- Compose PostgreSQL 曾因过长 revision ID 写入 `alembic_version.version_num` 而报 `psycopg.errors.StringDataRightTruncation`；修复后的 `0006_assets_legacy_repair` 已在真实 Compose PostgreSQL 完成 `0005 -> 0006 -> 0005 -> 0006` cycle。
- Node 使用根 `pnpm` workspace 与 `pnpm-lock.yaml`；Python API 固定 Python `>=3.12,<3.13` 并使用 `services/api/uv.lock`。
- 运行边界为 `apps/web`、`packages/contracts`、`packages/ui`、`services/api`、`workers/{agent,generation,media}` 与 `infra/compose`。
- Compose 已验证 Web、API、PostgreSQL 18、Temporal 与三类 Worker；API 和 Worker 通过同一严格 runtime composition 消费示例配置，默认只使用 Mock Provider 和 LocalWorkspaceAdapter。
- 阶段 0 仍保留健康/runtime/ports 等迁移起点；阶段一 20 个 change 的实现范围仍保留在工作区，任务均已完成且尚未归档。仍不得把缺少外部账号/凭据的 live Provider、TOS 或 FFmpeg 能力误写为成功，必须保留 `unconfigured`/503 结果。

## 当前可证实目录

- `docs/agent/`：本目录，保存项目持久记忆。
- `docs/adr/`：架构决策记录。
- `docs/phase-zero-traceability.md`：R1-R8 到规格、实现和验证的追溯。
- `openspec/`：OpenSpec 配置与变更 artifacts。

## 待确认

以下信息当前没有可验证的项目文件，必须在得到证据或用户确认后再记录为事实：

- 真实 Provider、TOS、生产凭据和远程部署策略仍未实现或验证。
- 完整生成、媒体处理、专业剪辑、多人协作、手机端和发布平台仍为后续阶段范围；真实 Provider/TOS 仍未实现或验证。

## 使用规则

此文件只记录稳定且已确认的项目事实。与当前代码、测试、schema 或可执行配置冲突时，以后者为准并更新本文件。
