# 当前交接

## 当前状态

- 当前分支：`main`。
- **阶段一组件库原型已获视觉确认**：`/prototype` 只使用 `shared/ui`、Radix、Tailwind token 与静态演示数据，不调用 API。确认的桌面布局从 `lg` 起为左侧完整菜单列、右侧独立工作台；左栏只显示产品标识和紧凑项目导航，项目名与集上下文属于右侧工作台顶部。工作台内的拍摄板、镜头工作区和版本检查分列，镜头工作区承载主内容滚动，整页不滚动。
- **阶段一正式 UI 迁移已完成**：`redesign-phase-one-workbench-ui` 已在用户确认后完成共享壳层与六类页面迁移。`app/query-client.ts`、`layouts/ProjectShell.tsx`、路由页面和 `workbench/` 功能域均按 `shared/ui`、Tailwind token 与 Lucide 组合；Review、Assets、Timeline、Exports、Provider/Storage Settings 的历史页面 class 已清理。`/projects` 保留全局菜单并按内容高度结束；带项目 ID 的深链继续保持全屏工作台布局，宽屏滚动画布使用完整可用宽度，当前剧集选择已合并到标题上下文区。owner API、project scope、CAS、显式命令、`unconfigured` 语义与现有测试保持不变；AssetEdit 与 TextReview 的 project-scoped ID-only 请求现在显式携带 `X-Project-Scope`，409 仍只触发权威重读而不自动重试。
- **已实现：阶段 0**：`establish-phase-zero-foundation` 已完成 35/35 tasks，`projects/episodes` 垂直切片已完成 19/19 tasks；两个 change 均已归档。
- **已实现：Assets/AssetVersions**：`implement-assets-asset-versions-slice` 已完成 20/20 OpenSpec tasks，包含领域、应用、内存/SQLAlchemy adapter、HTTP API、可逆 Alembic `0004`/`0005` 和 `0006` legacy workspace reference repair；独立 `repair-assets-object-key-contract` 以共享机器可读 corpus 统一 Schema/domain/HTTP/`0004`/`0006` 的 canonical objectKey 合同，新增拒绝 `workspace:`/`workspace:/`/`s3:`、空 `?`/`#` 分隔符与 Python `strip()` whitespace（含 newline/U+0085）。历史 migration 使用冻结本地 helper，持久化其 canonical 值；`0006` 只修复完整 workspace reference，不改写普通 legacy provider/bucket。历史 `sol_max_closure` 仍有效 fail，旧 lineage 保持 `scope_decision_required`；用户授权的 `complete-assets-asset-versions-slice-v3` replacement lineage 已由独立 `sol_xhigh` 有效审核为 pass，controller `close_check` 返回 `close_allowed=true`。相关 change 已完成并归档，replacement 结果不改写旧 lineage。
- **已定义：接口/目标架构**：`define-backend-engineering-architecture` 已完成 7/7 tasks；目标后端目录、模块所有权、分层依赖、Unit of Work/Repository/Outbox、HTTP/Temporal/Worker 边界、测试分层和阶段 0 迁移规则已同步到 Markdown 与技术实施方案 DOCX。目标架构仍按切片逐步迁移。
- **待实现：产品能力**：真实 Provider/TOS、FFmpeg、SSE、Outbox、专业剪辑、协作、移动端和平台发布仍未实现；阶段一的 Mock/Local 媒体 owner、Timeline、Export 与 Worker 代码已实现，但真实文本 Provider/TOS/renderer 仍需显式 opt-in probe。
- **阶段 1 当前进度**：当前未归档范围为 1 个总体协调 change 与 19 个 child，共 20 个 changes、495 项 tasks；正式 UI 迁移 change `redesign-phase-one-workbench-ui` 已同步主规格并归档。`prototype-shadcn-workbench` 不存在于当前制品，不得恢复或创建。所有任务已按历史实现与本轮 UI/集成验证同步完成。Storage 使用 `0019_storage_owner` 和可恢复 Local multipart，Timeline/export 使用 `0020_timeline_export_owner` 与 `0023_export_dispatch_owner`，资产中心使用 `0022_asset_center_owner`；仓库与当前 Compose PostgreSQL Alembic head 均为 `0023_export_dispatch_owner`。真实 TOS、Provider credentials 与 renderer 前置缺失分别保持 `unconfigured`、503 和 `renderer_unconfigured`。
- **阶段一退出证据**：`docs/evidence/E2E-MVPA-001.json` 已逐行记录 16 个 canonical stages；Review 专项为 `docs/evidence/E2E-MVPA-001-context-agent-review.json`，2 GiB、resilience、observability 专项文件仍保留在同一目录。
- **已实现：Project/Episode creative、结构化文本、Scene/Shot 与 AssetBible**：Project/Episode owner 以 revision CAS 接受 StorySpec/ScriptSpec handoff；AgentScope/Skill route 只生成结构化 StorySpec/ScriptSpec/Scene/Shot/ShotSpec 和实际引用的初始 AssetBible specs；Scene/Shot owner 通过关系表拥有 ShotSpec、排序、handoff 与媒体 current CAS；AssetBible owner 通过关系表拥有六类 typed entry/immutable version、四层 override、resolved snapshot、impact/AcceptDecision/revision task 与幂等 handoff ack。全部 owner ack 前媒体门保持关闭，任何 consumer 都不能直接改 AssetBible 或自动重生成下游。
- **已实现：阶段一 Skill 注册、路由与 Run 冻结语义**：AgentScope 2.x 是 Agent Worker 独立 runtime dependency；八项 Registry candidate 中默认 Workflow 只绑定 approved 的 `novel-writing` 与 `drama-skills`，其他六项保持 pending/disabled。Worker 启动只读取 Registry 索引与 approved metadata，路由后才按需读取固定 revision；Git 与公开 Markdown 使用不同 source identity。Run 创建、successor 与 historical rerun 均重新验证持久 route candidate、approved/enabled catalog revision 和统一 upstream SHA-256 digest，并从 catalog 冻结 Provider/Profile/Model/各 operation capability snapshot；客户端 selection 不能绕过 owner route。
- **已修复：Run/Text/Video/Export 跨 owner 运行链**：Agent Worker 现在消费持久 `TemporalStart` 并以稳定 workflow ID 启动 `phase_one_run`，成功后推进 matching NodeRun；Text 仅允许 running Run 的 running `text.generate` NodeRun，强制 frozen model/capability，并拒绝不完整、计数/编号/层级或 AssetBible closure 不匹配的模型图；Video submit 强制 Run/project/node、frozen selection、当前 enabled catalog capability 与 confirmed BudgetGate，reconcile 仅通过持久 provider request 查询 `submission_unknown`；ProviderCall 保存真实 capability ID，export provenance 可与 VideoOperation/Run/catalog 精确闭合。Phase-one scope 入口缺失 `X-Project-Scope` 即返回 403。
- **已修复：阶段一 review 数据/授权边界**：creative/scenes/text/video/agent-edit/Asset Center 统一强制 `X-Project-Scope`，ID-only route 按实体 owner 再校验；Temporal activity 完成 TextGeneration -> TextReviewBatch -> `waiting_review`；PhaseOneDocument 更新使用 loaded-revision CAS；SourceMaterial binding/uploaded AssetVersion 来源事实不可由请求伪造；ProviderCall durable claim 在 ambiguous 状态禁止重复提交；Local canonical key、multipart 完整 binding、delete proof fail-closed 与 API/Media shared workspace volume 均有回归测试。
- **已确认：阶段一 retention/no-GC**：workflows/runs、AssetEdit、catalog、AgentScope、资产中心和 operations resilience 的 owner/清理边界已实现并通过测试。`RunEvent`、`AcceptDecision`、`CapabilitySnapshot`、脱敏 `ProviderCall` 摘要和仍被引用的 `AssetVersion` 不得被诊断过期、temporary/derivative cleanup、容量维护、恢复或 GC 删除、覆盖或静默压缩；只有明确无引用且符合 policy 的临时对象可清理。
- **已冻结：阶段 1 关键行为**：一次运行先生成完整文本候选图并逐对象通过 Schema 校验，再执行一次必需的批量文本审核；批次内上游修改使依赖候选 stale，补齐并重新校验后才能用精确候选/引用集合 CAS 在同一事务全有或全无地接受，接受前暂停付费媒体。Provider 优先级为 workflow node > project default > enabled system default，Run 冻结 snapshot 且无隐式 fallback；`submission_unknown` 先 reconciliation，取消晚到结果为未引用候选。详细字段、媒体、TOS、Skill、加密、保留、FFmpeg 和退出矩阵以 [总体设计](../../openspec/changes/plan-phase-one-drama-mvp-a/design.md) 与各 child artifacts 为准。
- **已补齐：阶段 1 focused gap 追溯**：历史 snapshot 新 Run、Episode presentation/session isolation、SoundCue、ExportDiagnosticTarget/packaging、实际 2 GiB、W3C trace/log/metric、localhost-only/LAN MVP-B 等合同均已同步到 child tasks、ADR 和证据文件；PRD `cueType` 只映射为 canonical `track`，automation/keyframes 延后 MVP-B。
- **已冻结：`E2E-MVPA-001` 阶段证据矩阵**：总体 `design.md` 以 `S01`-`S11` 加不重编号的 `S03a historical rerun`、`S04a asset bible continuity`、`S08a asset center`、`S08b 2 GiB media chain`、`S11a observability` 固定 owner、exact prerequisites、success evidence、focused failure 与 no-side-effect invariant；spec/tasks/UI 只引用该矩阵。实现前仍须以当前代码/Schema/Alembic head 重新取证。
- 当前 Compose 环境保持运行且七类服务 healthy：Web `http://127.0.0.1:5174`、API `http://127.0.0.1:8000/v1/health/ready`。`5173` 被本项目外监听者占用，因此本项目使用 `5174`。
- 仓库与当前 Compose PostgreSQL head 均为 `0023_export_dispatch_owner`。`0018` 增加 AssetEdit owner 表，`0019` 增加 Storage owner 表，`0020` 增加 Timeline/Media/Export owner 表，`0022` 增加 Project Asset Center owner 表，`0023` 增加 `export_dispatch_outbox` 与 ExportJob 冻结执行快照；Timeline/export + Assets SQLite migration cycle `77/77`、临时 PostgreSQL `head -> 0022 -> head` 与当前业务库 `0022 -> 0023` 均通过，`Base.metadata.create_all` 的持久 batch/dispatch restart 回归通过。临时 PostgreSQL 测试库已删除。
- `uv run alembic check` 在 `services/api` 目录仍会报告历史 owner/document migration 与当前 SQLAlchemy `Base.metadata` 的声明漂移；该命令不是阶段一退出门，详细可复现证据见 [TROUBLESHOOTING.md](TROUBLESHOOTING.md)。不得用 autogenerate 过滤器或未经设计的漂移 migration 伪造 clean；后续需独立 schema reconciliation change。

## 已完成验证

- Storage focused 验证通过：架构/contract/domain/application/HTTP/adapter/migration/worker tests；实际 2 GiB 有效 PNG 为 32×64 MiB 分片，第 1 片后重建 Adapter 恢复，observed bytes/checksum/ETag/MIME 一致，只登记 1 个 AssetVersion，Media Worker 成功 inspection 并生成 68-byte proxy；报告在 `/tmp/phase-one-storage-2gib-evidence.json`，媒体临时目录已删除。真实 TOS 的 SDK/version/account/private bucket/Docker Secret/credential 均无可验证输入，因此结果为 `unconfigured` 且未发 live request。
- `implement-project-asset-center` 已完成 39/39：Asset catalog/metadata CAS/reservation/Storage/Media/usage/Timeline handoff 与共享 DataTable/Tabs/VirtualList、领域音频播放器已落地；Playwright 从共享导航进入资产中心，验证同一 reservation/session 恢复、真实 PNG/WAV 上传、单一版本、筛选、usage 和 exact Timeline handoff。`S08a/S08b` 证据位于 `docs/evidence/E2E-MVPA-001-asset-center*.json`；Alembic head 为 `0022_asset_center_owner`，七个 Compose 服务 healthy。真实音频派生、TOS 与 FFmpeg 保持 `unconfigured`。
- Playwright CLI 的 `.playwright-cli/` 运行时日志/页面快照/截图，以及 `apps/web/output/`、`apps/web/test-results/` runner 产物已加入版本忽略；根 `output/playwright/` 是阶段退出证据，继续保留并由 `docs/evidence/` 引用；`.playwright/cli.config.json` 与 `apps/web/playwright.config.ts` 仍作为项目配置保持可见。
- 最近一次阶段一验证：Contracts 78、Web 75、API 432；Web typecheck、lint（10 条库/fast-refresh warning，0 errors）和本轮页面 format 均通过；Chrome/Edge E2E `2/2`、20 个未归档 changes strict validation、`git diff --check` 与 Compose 七项服务健康均通过。本轮镜像已使用当前源码重建。Compose config 使用 `.env.example` 展开并确认 API/Media Worker 共享 `local-workspace`、Agent/Media Worker 注入 `DATABASE_URL`；业务库为 `0023_export_dispatch_owner`。
- replacement workflow 验证与关闭门均已通过：OpenSpec apply/status/strict、`pnpm run check`（Contracts 32、Web 2、Python 214）、`git diff --check`、Compose config/`up --build -d --wait` 与七服务 health 均通过。真实 PostgreSQL 已再次验证 `0005 -> 0006 -> 0005 -> 0006` 并回到 `0006_assets_legacy_repair`；HTTP smoke 为 health 200、合法 Asset/AssetVersion create 201/read 200、`workspace:projects/a/v1.wav` 与 `projects/replacement?` 均为 422，且 `contentHash`/`checksum` 独立、响应无媒体 bytes。源码和当前 Compose 日志扫描只命中本地 health probe、显式未配置 TOS adapter 与测试脱敏字段，没有真实 Provider/TOS 或付费调用。独立 `sol_xhigh` 覆盖 A1-A7 后有效 pass，claim 为 `bab28b17-a3f6-4fbb-a954-8f92c1a3f2df`；controller `close_check` 返回 `close_allowed=true` 并释放 workspace lease。
- Alembic 已在临时和 Compose PostgreSQL 验证 `0001 -> 0005`、`0005 -> 0004 -> 0005` 以及修复后的 `0005 -> 0006 -> 0005 -> 0006`；最终 head 为 `0006_assets_legacy_repair`。Compose 真实 HTTP smoke 已创建 Project/Asset/AssetVersion，确认独立 contentHash/checksum、无媒体 bytes；七类服务均 healthy。当前 Compose 不自动执行 migration，新空卷启动前仍须显式执行 `alembic upgrade head`。
- Compose API 真实 HTTP 已验证：health ready 200、项目/剧集创建成功、重复编号 409、正确 `If-Match` 更新 revision、过期 revision 409。
- API 镜像的无缓存 frozen lock 构建通过；隔离副本中的 `pyproject.toml`/`uv.lock` 漂移会在 `uv lock --check` 处失败。
- API 与三类 Worker 共用严格 runtime composition；示例配置实际选择 Mock Provider/LocalWorkspaceAdapter，未知模式显式失败，Provider/Storage 边界日志及 Skill manifest 严格准入均有正反测试。
- `docker compose --env-file .env.example -f infra/compose/compose.yaml config`、镜像 build、`up --wait`、API/Web HTTP 探测均通过。
- Assets HTTP smoke 已通过：health 200、OpenAPI 使用 `{projectId}/{assetId}/{versionId}`、创建/读取 AssetVersion 保持独立 `contentHash` 与 storage `checksum`、missing asset 404；响应无媒体 bytes。
- 范围扫描仅发现健康探针、Schema `$id` 和显式未配置 TOS 测试引用；没有真实 Provider/TOS 客户端或付费调用。
- 后端架构文档变更的 DOCX ZIP/关键段落/表格几何/a11y 结构检查通过；当前 Windows 环境没有 `soffice`，因此未完成 PNG 渲染和逐页视觉 QA。

## 待确认

- 真实 Provider、TOS、加密服务、生产密钥与远程部署均尚未实现或验证；TOS 已有独立的 `integrate-tos-storage-provider` 规划，真实调用仍需 explicit opt-in profile/probe。未提供 Docker Secret、账号许可、allowlist 或 `ffmpeg`/`ffprobe` 输入时，必须保留 `unconfigured`/503 与原始错误，不能报告成功；Agnes MVP-A 不接入 callback/webhook。
- Compose 使用开发数据库凭据，仅限本地环境；不得作为生产密钥或配置复用。
- Compose readiness 只验证数据库连通性；新空卷不会自动执行 Alembic，部署/启动脚本接管 migration 需由后续独立 change 固化。
- 旧 `sol_max_closure` 的产品 blocker 已在独立 `repair-assets-object-key-contract` 中实施：Schema、domain、HTTP 与 `0004`/`0006` 通过共享 corpus 一致拒绝 `assets/a/`、Python whitespace、`workspace:`/`s3:`、空 query/fragment delimiter 及 `C:relative.mp4`；历史 migration 不导入可演进 runtime helper，且不以 SQL `trim()` 重解析 raw value。旧 controller lineage 仍不得作为关闭依据并保持 `scope_decision_required`；该 change 的完成依据是已通过的 replacement lineage。
- `sol_max_closure` 的 controller blocker：旧 task `assets-asset-versions-slice-v2` 缺少完整 claims、repair records 和 max charter，不能声明 `close_allowed`。root 读取 audit-context 时可见空记录，而 closure reviewer 的实例返回 `LEGACY_STATE_MIGRATION_REQUIRED`；两条证据都不支持关闭。不得删除、迁移、read-through 或伪造该 state。
- 既有 `projects/episodes` OpenAPI 仍暴露 snake_case 路径参数，属于本轮 assets 之外的后续契约修复范围。
- `implement-episode-timeline-audio-export` 31/31 与 `implement-episode-timeline-editor-ui` 30/30 已完成。Timeline Editor 使用可调分区、同父 Clip pointer/keyboard 排序、虚拟 Clip/日志/版本列表和领域 PixiJS/WaveSurfer.js/HLS.js 预览；写 command 均携带当前 revision，跨父移动与真实 renderer 均不在范围。Export batch 持久 outbox 由 Media Worker dispatcher 启动稳定 Temporal workflow/activity；真实 renderer 因未提供 `FFMPEG_PATH`/`FFPROBE_PATH` 保持 `renderer_unconfigured`。

此文件只保留可继续执行的当前状态；任务完成后应替换过期内容，而非追加流水账。
