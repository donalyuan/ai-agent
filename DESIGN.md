# VEDIO-AGENT 工作台设计系统

## 视觉方向

`VEDIO-AGENT` 是面向运营人员的视频工作台，正式实现归属 `apps/video-agent/`。界面应安静、紧凑、可靠：分区清晰、控件可预测、列表便于扫描、异步状态明确可见。它不是营销页。

`admin/` 是 Novex 平台管理后台，不使用本工作台页面作为首屏，也不承载脚本生产、素材匹配、视频生成、发布排期或优化建议等日常视频生产流程。

视频工作台 Pencil 原型源文件统一保存在 `docs/prototypes/video-agent/video-agent.pen`。后续视频工作台原型修改必须更新该 `.pen` 文件，不再使用 `docs/prototypes/script-agent-workspace/` 截图目录作为原型来源。

默认使用浅色中性背景。颜色只用于状态、优先级和主操作。避免装饰渐变、hero 区块、大插画、嵌套卡片和硬编码项目入口。

## 设计系统参考

借鉴这些真实设计系统：

- `Ant Design`：后台工作台分栏、紧凑表单、筛选控件、列表密度和操作反馈位置。
- `IBM Carbon`：严谨表单分组、可访问标签、错误提示、加载状态、空状态和克制间距。
- `GitHub Primer`：低干扰边框、状态标签、列表行操作和中性表面。

拒绝这些模式：

- 营销 hero、超大字号、装饰渐变背景和大面积宣传面板。
- 应由结构分区承载的页面被堆成多层卡片。
- 单一紫蓝、深蓝、米色或橙棕主导配色。
- 隐藏或硬编码 `project_id`，绕过真实项目选择或创建流程。

## 颜色令牌

默认视觉语言使用这些语义角色：

- `--color-canvas`: `#f6f8fb` 页面背景。
- `--color-surface`: `#ffffff` 主面板和重复项。
- `--color-surface-muted`: `#eef2f6` 次级控件和弱化区域。
- `--color-border`: `#d8dee8` 默认边界。
- `--color-border-strong`: `#b8c2d1` 选中或聚焦边界。
- `--color-text`: `#172033` 主文本。
- `--color-text-muted`: `#5d6878` 次级文本。
- `--color-text-subtle`: `#7d8796` 元信息。
- `--color-primary`: `#2563eb` 主操作和聚焦色。
- `--color-primary-hover`: `#1d4ed8` 主操作悬停色。
- `--color-success`: `#1f8a4c` 已通过或健康状态。
- `--color-warning`: `#b7791f` 草稿或待处理状态。
- `--color-danger`: `#c2413a` 失败或风险状态。
- `--color-info`: `#2f6f8f` 信息状态。

状态标签使用浅色背景、深色文字和边框，不使用高饱和实心胶囊：

- 草稿：背景 `#fff6db`，文字 `#7a5100`，边框 `#efd27a`。
- 已通过：背景 `#e8f6ee`，文字 `#17633a`，边框 `#9fd3b4`。
- 已归档：背景 `#f2f4f7`，文字 `#4f5867`，边框 `#cfd6df`。
- 错误：背景 `#fdecec`，文字 `#9f2f2a`，边框 `#f4b9b5`。

## 字体

字体栈使用 `Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`。

文本层级：

- 页面标题：22-24px，700，line-height 1.25。
- 区块标题：15-17px，700，line-height 1.35。
- 正文：14px，400，line-height 1.55。
- 元信息和辅助文本：12-13px，500 或 400，line-height 1.45。
- 按钮和标签：13-14px，600。

不要用视口宽度缩放字号。字距保持 `0`。

## 布局

桌面工作台布局：

- 顶部栏：56-64px 高，显示产品名 `VEDIO-AGENT`、二级标题“视频工作台”、服务状态和项目选择。
- 主体：左侧全局业务流程菜单加当前业务工作区，不做卡片套卡片。
- 全局业务流程菜单：固定在主体左侧，由数据库菜单配置驱动，一级菜单为内容策略、脚本创作、素材管理、作品生产、发布运营、数据分析、工作流任务；当前高亮脚本创作，未实现菜单展示计划态且不可进入。
- 当前业务工作区：承载脚本创作下的脚本生成、脚本列表、时间轴详情和状态操作；底层 Agent 只作为二级菜单、模块状态或执行状态展示。
- 脚本列表面板：状态筛选、脚本标题、分镜数和创建时间，宽度 260-320px。
- 脚本详情面板：使用“时间轴 + 旁白/画面对照”视图展示脚本节奏和镜头推进，弹性宽度。
- 生成与操作面板：生成表单和状态操作，宽度 300-360px。
- 桌面页面边距：20-24px。
- 面板间距：16px。

本 change 只覆盖桌面端运营后台。`script-agent-workspace` 不包含移动端原型、移动端适配或移动端验收；后续如需要移动端，应单独提出 OpenSpec change。

## 组件

### 按钮

主按钮使用 `--color-primary`、白色文字、6px 圆角、最小高度 40px。次级按钮使用白底、`--color-border` 边框和 `--color-text` 文字。破坏性或失败操作优先使用危险色文字或边框，避免直接使用高饱和填充。

工具型操作有合适图标时应带图标。纯文本按钮只用于清晰命令，例如创建项目或生成脚本。

### 表单

每个输入都必须有可见标签或可访问名称。相关字段使用紧凑垂直分组。校验提示显示在字段附近或所属面板内。提交失败必须保留用户输入。

控件模式：

- 项目选择：选择器或组合框。脚本生产工作台不得展示项目创建或项目管理入口。
- 脚本状态筛选：segmented control，界面显示“全部、草稿、已通过、已归档”。
- 风格选择：segmented control 或 select，界面显示“知识型、故事型、教程型”；接口值仍为 `knowledge`、`story`、`tutorial`。
- 分镜数量：数字步进器或 select，限制为 3-12。

### 列表

脚本列表行应稳定、便于扫描。展示标题、状态标签、分镜数和创建时间。长标题最多换两行或在固定容器内截断，不能撑破相邻控件。

### 详情视图

详情面板展示标题、状态、总时长和有序分镜。脚本详情采用时间轴对照视图：左侧展示分镜序号和节奏节点，右侧并排展示旁白和画面指令。每个分镜必须展示序号、旁白、画面描述、情绪和时长；需要卡片承载时圆角不超过 8px。

### 反馈状态

状态反馈绑定到工作流所在位置：

- 接口不可用：顶部状态提示，同时禁用写入型操作。
- 无项目：创建项目是主内容状态。
- 无脚本：脚本列表展示空状态，同时生成面板保持可用。
- 生成中：按钮尺寸稳定，显示加载文案，禁止重复提交。
- 生成失败：错误提示在生成面板内，用户输入保留。
- 状态更新失败：错误提示在详情或操作区域内，原状态保留。

## 可访问性

聚焦态使用 `--color-primary` 且必须可见。所有文字和状态标签保持足够对比度。加载和错误消息在实现时应能被屏幕阅读器感知。

## 实现护栏

- 管理后台首页不要做落地页。
- 不使用 hero 区块、装饰渐变或背景光斑。
- 不做 UI 卡片嵌套。
- 动态标签、加载文案和长内容不能造成面板布局位移。
- 除非实现范围明确需要，不引入新的大型设计库。
