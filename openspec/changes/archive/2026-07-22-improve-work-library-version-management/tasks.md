## 1. 原型确认与测试基线

- [x] 1.1 使用项目 `DESIGN.md`、Ant Design、IBM Carbon、GitHub Primer 参考完成作品详情 v2 Pencil 原型，并通过截图和布局检查
- [x] 1.2 取得用户对“桌面宽屏 - 作品详情与版本分组 v2 待确认”的明确开发确认
- [x] 1.3 补充重复规划、派生草稿复用、历史清理和详情分组的失败测试基线

## 2. 草稿版本治理

- [x] 2.1 增加 migration，规范无当前版本作品的保留草稿，并按完整安全谓词清理冗余失效草稿
- [x] 2.2 重构 `save_plan` 事务，锁定作品并复用当前未运行草稿，同时更新版本快照和 `current_version_id`
- [x] 2.3 让 `edit` 与 `full_regeneration` 按来源和派生类型复用已有草稿
- [x] 2.4 增加同作品并发规划测试，确认同一生产意图只生成一个版本
- [x] 2.5 验证失败运行、调用尝试、产物、时间轴、差异、发布和来源引用均阻止历史清理

## 3. 作品详情重构

- [x] 3.1 用显式业务字段映射替代首屏递归 snapshot 展示，并实现默认折叠的完整技术快照
- [x] 3.2 根据轨道数据实现紧凑空时间轴和完整多轨时间轴两种状态
- [x] 3.3 实现当前草稿、可用成片、失败与早期记录分组及准确计数，历史组默认折叠
- [x] 3.4 按 v2 原型重构详情布局为单一主滚动上下文，移除主区与侧栏竞争滚动
- [x] 3.5 保持失败版本任务入口、版本对比、编辑、重生成、下载和发布操作按所选版本正确显示

## 4. 验证与交付

- [x] 4.1 在容器内运行相关 Rust repository、database 和 route 测试
- [x] 4.2 在容器内运行 Video Agent 前端单测和作品库 E2E
- [x] 4.3 运行 Rust workspace 全量测试，确认 migration 与其他领域引用无回归
- [x] 4.4 执行 `openspec instructions apply --change "improve-work-library-version-management" --json` 并确认任务状态与实现一致

## 5. v3 Agent 对话与原型一致性

- [x] 5.1 完成“桌面宽屏 - 作品详情 Agent 对话 v3 半版待确认”原型并取得用户明确开发确认
- [x] 5.2 补充作品 Agent 归属、结构化补丁、草稿复用、diff 和失败测试
- [x] 5.3 扩展 `work` Agent Runtime，完成同一草稿修改与待确认 diff
- [x] 5.4 前端用作品 Agent 对话替换原始提示词、保存和分析入口
- [x] 5.5 严格按 v3 原型重构详情业务工作区

## 6. v3 验证与交付

- [x] 6.1 在容器内运行相关 Rust 测试及 workspace 全量测试
- [x] 6.2 在容器内运行前端单测、lint、build 和作品库 E2E
- [x] 6.3 使用 Playwright 在 `1920x980` 对照 Pencil 原型完成视觉验收
- [x] 6.4 执行 OpenSpec apply 检查并确认任务状态与实现一致
- [x] 6.5 修复作品 Agent 输入区继承全局焦点样式导致的双重边框变形，并补充 E2E 回归验证
- [x] 6.6 修复历史版本展开后列表收缩导致操作区覆盖版本卡片，并补充 E2E 几何回归验证
- [x] 6.7 修复历史版本展开后详情 Surface 固定高度导致卡片越界，并验证版本栏与外层边框完整包裹内容
- [x] 6.8 按用户确认改为固定详情高度和历史记录限定滚动，并验证固定区、滚动区及无覆盖
