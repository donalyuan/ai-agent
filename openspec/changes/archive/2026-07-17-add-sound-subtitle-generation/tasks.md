# 声音与字幕生成实施任务

## 1. 协议与能力目录

- [x] 1.1 对照实施时有效的官方文档核验 `ListSpeakers`、TTS V3 和 ASR 请求响应并建立脱敏 contract fixture。
- [x] 1.2 先补目录同步失败测试，再实现按 `ResourceID` 分页、完整批次原子提交和下线标记。
- [x] 1.3 实现 Admin 主动/定期同步、工作台检查更新和同步状态可观测。
- [x] 1.4 先补统一模型管理回归测试，再扩展 `speech` 类型、TTS/ASR 协议、语音凭据脱敏及旧类型兼容。
- [x] 1.5 实现 Admin 语音模型新增/编辑/生命周期管理，以官方术语区分运行 X-Api-Key 与目录 OpenAPI AK/SK，维护版本化声音能力及失效选择保留但阻断生成。
- [x] 1.6 将 Admin TTS 时间戳语言改为中文可搜索多选下拉，支持外部点击与 `Escape` 关闭，保证至少一项并保持标准代码提交；ASR 显示只读自动识别。

## 2. Agent、TTS 与试听

- [x] 2.1 先补声音 Agent adapter 测试，再接入统一对话 Runtime 和工具步骤审计。
- [x] 2.2 实现 TTS V3 provider、流式校验、唯一请求 ID、`X-Tt-Logid` 和密钥脱敏。
- [x] 2.3 实现主动试听、字符数确认、幂等、并发限制和临时错误最多一次重试。
- [x] 2.4 实现音频落盘和素材库新增，不覆盖历史素材。

## 3. 字幕与 ASR

- [x] 3.1 对照官方 TOS Python SDK 建立上传、短期签名、删除和脱敏 contract fixture。
- [x] 3.2 实现 TTS 字词时间戳断句、时间轴和 SRT 输出。
- [x] 3.3 实现不支持时间戳语种的明确失败，不伪造对齐。
- [x] 3.4 实现 `ffprobe` 音频检查、已有音频幂等中转、ASR 任务、字幕素材、终态清理及失败补偿审计。

## 4. 页面与验收

- [x] 4.1 更新 Pencil 双标签工作区原型并取得用户明确确认。
- [x] 4.2 实现动态模型/音色/风格选择、声音 Agent 对话和任务状态。
- [x] 4.3 补齐目录、provider、API、Worker、前端和桌面端 E2E。
- [ ] 4.4 经用户单独许可后执行最小受控真实 TTS/ASR 验证。
- [ ] 4.5 运行 OpenSpec 和全量相关验证后申请归档。

> 归档说明（2026-07-17）：用户已明确确认放弃 4.4 与 4.5，并要求在保留未完成状态和风险记录的前提下强制归档、提交并推送。已完成的自动化验证不等同于真实 TTS/ASR 成功验收。
- [x] 4.6 将工作台音色选择改为展示真实原名、中文描述和中文标签的可搜索单选组合框，完整映射真实语言代码且禁止使用试听文案作为语言名称，并补齐组件与桌面端 E2E。
- [x] 4.7 按已确认 Pencil `z6UBe` 重构整页桌面工作区：恢复页头层级与 `250/520/276px` 三栏，将任务列表移入左栏，补齐中栏试听/任务状态和右栏 Agent 审计，删除底部重复任务表，并通过结构与像素 E2E。
- [x] 4.8 将语言改为在触发框下方展开的自定义单选，按官方 V3 契约移除无效情绪必填与 `language/emotion` 请求字段并改用 `explicit_language`；宽屏保持已确认原型 `250/520/276px` 三栏和内部控件尺寸，仅居中整个工作区以平衡留白，补齐前后端、Worker 和桌面 E2E。
- [x] 4.9 按最新确认宽屏原型实现 1920px 下 `1598px` 与 `250/1000/276px` 布局、宽屏中栏固定内部尺寸、1440/1920 基准间连续利用中栏剩余宽度，以及音色语言/声线 Tag 交集筛选和扁平搜索列表，并补齐组件与桌面 E2E。
- [x] 4.10 修复旧版通用样式覆盖右栏 Agent 两行头部的问题，将未配置 `speed_ratio.default` 的初始语速固定为合法范围内的 `1.0`，并补齐组件与桌面几何 E2E。
- [x] 4.11 将 TTS 模型选择从浏览器原生 `select` 改为与触发框同宽、在正下方展开的自定义单选弹层，并补齐键盘交互、组件与桌面几何 E2E。
- [x] 4.12 按已确认 Pencil `b3HsYi` 先补 Worker、migration、API、组件与 E2E 失败测试，再实现供应商非 2xx 结构化脱敏诊断、完整追踪信息失败态，以及左栏多任务固有卡片高度和独立纵向滚动。
- [x] 4.13 按已确认 Pencil `wl2QV` 先补组件与桌面 computed-style 失败测试，再实现正文不低于 `12px`、辅助信息不低于 `11px`、任务/表单/按钮与失败标题 `13px`、旁白 `14px`、失败消息/诊断 `12/11px` 的可读字号层级，并复验既有三栏尺寸、宽屏失败详情和滚动边界。

## 5. 系统 TOS 工具解耦

- [x] 5.1 更新 Admin Pencil 原型，将私有 TOS 移入“工具与 MCP”，移除 ASR 模型可编辑 TOS 字段并取得用户明确确认。
- [x] 5.2 先补迁移、系统工具 API、模型 CRUD 解耦和任务版本锁定失败测试，再新增独立版本化 TOS 配置表并删除 `ai_models` staging 字段。
- [x] 5.3 实现系统 TOS 管理 API、凭据脱敏/留空保留、乐观锁、连接状态和待清理对象变更约束。
- [x] 5.4 改造 ASR 任务与 Worker/清理器只读取任务锁定的系统 TOS 配置版本，删除模型 staging 兼容路径。
- [x] 5.5 实现 Admin“工具与 MCP / 私有 TOS”配置页、连接检查和状态展示，并从模型 DTO、表单与测试中移除 `staging_config`。
- [x] 5.6 运行 Backend、Worker、Admin、E2E 和 OpenSpec 全量相关验证，确认解耦任务与实际进度一致。

## 6. 工作台路由持久化

- [x] 6.1 先补菜单 URL、深层路径恢复、浏览器前进/后退和无效路径回退测试，再实现基于数据库 `route_path` 的 Next.js 工作台路由并完成构建/E2E 验证。

## 7. 音色目录 Worker 解耦

- [x] 7.1 先补独立开关、健康状态、目录/语音任务隔离、供应商空数组归一化和失败调度退避测试，再实现 `VOICE_CATALOG_WORKER_ENABLED` 独立轮询并受控消费当前目录任务。

## 8. 脚本创作旁白导入

- [x] 8.1 先补脚本来源校验、版本失效、快照持久化和重试复制测试，再扩展声音任务 migration、DTO、应用服务与 Repository。
- [x] 8.2 实现当前项目 `draft | approved` 脚本搜索、分镜选择、超限阻止和明确替换交互，并保留导入后的可编辑来源状态。
- [x] 8.3 补齐 API、组件与桌面端 E2E，运行相关 Backend、前端和 OpenSpec 验证。

## 9. 同上游模型音色目录复用

- [x] 9.1 先补 migration、模型 CRUD、目录解析和来源生命周期失败测试，再实现官方同步/共享目录来源持久化与稳定业务约束。
- [x] 9.2 补 Admin 组件与 E2E 测试，再实现目录来源模式、来源模型选择、共享模式隐藏 AK/SK 及列表来源状态。
- [x] 9.3 补工作台预检快照与 Worker 调度回归，运行 Backend、Worker、Admin、video-agent、E2E 和 OpenSpec 相关验证。

## 10. OpenAI Audio Speech 中转

- [x] 10.1 先补 migration、模型 CRUD、跨协议目录绑定与字幕能力阻断测试，再扩展 `openai_audio_speech` 注册和持久化约束。
- [x] 10.2 补 Admin 与工作台组件测试，再实现中转协议表单、强制共享目录、Bearer 凭据和不支持 TTS 时间戳的可见状态。
- [x] 10.3 先补 Worker provider contract，再实现 `/v1/audio/speech` 标准请求、二进制音频校验、错误分类和任务调度。
- [x] 10.4 运行 Backend、Worker、Admin、video-agent、E2E 和 OpenSpec 相关验证，确认任务与实际进度一致。
- [x] 10.5 先补从非语音标签和任意列表筛选进入中转表单的失败测试，再将目录来源候选改为独立加载全部启用语音模型，区分加载中、失败可重试和无匹配状态，并完成 Admin 组件、E2E 与 OpenSpec 验证。
- [x] 10.6 先补中转 Bearer API Key 编辑提交、后端默认中转模型凭据更新、New API `X-OneAPI-Request-Id` 追踪和桌面抽屉首屏操作栏可见的失败测试，再按已确认 Pencil 三段式结构实现标题/操作栏固定、字段区独立滚动；配置拒绝必须返回安全中文校验分支，完成 Backend、Worker、Admin、E2E 与 OpenSpec 验证后再关闭。
