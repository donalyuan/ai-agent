## 1. 请求契约与测试

- [x] 1.1 为声音消息 API 补充当前编辑上下文的 DTO 校验和路由失败测试。
- [x] 1.2 为声音 Runtime 补充上下文进入 Prompt、模型不一致拒绝、完整目录不截断及试听文案排除测试。
- [x] 1.3 为声音工作台补充发送当前旁白、声音配置和字幕断句快照的前端测试。
- [x] 1.4 补充声音 Agent 使用 `json_object` 且 Prompt 携带完整输出契约的请求测试。

## 2. 后端实现

- [x] 2.1 扩展通用消息请求和 Runtime 输入，持久化并审计声音上下文且不影响其他 Agent。
- [x] 2.2 实现声音上下文模型绑定校验、完整紧凑音色目录和模型参数能力 Prompt。
- [x] 2.3 将声音 Agent 输出协议固定为 `json_object`，保留 Rust 严格解析与能力校验且禁止自动降级重试。

## 3. 前端实现

- [x] 3.1 定义声音上下文请求类型，并在每次发送时从当前编辑状态创建不可变请求快照。

## 4. 验证与治理

- [x] 4.1 在 Docker 容器内运行直接相关 Rust 测试与前端测试、lint 或构建验证。
- [x] 4.2 更新项目主题 memory，并确认 OpenSpec apply 状态与实际任务一致。
- [x] 4.3 运行相关测试、更新供应商协议诊断记忆，并再次确认 OpenSpec `all_done`。
