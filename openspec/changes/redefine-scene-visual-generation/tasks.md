# 画面生成边界重定义未来实施任务

> 本轮只定义规格。以下任务全部留待后续实施。

## 1. 行为冻结与回归测试

- [ ] 1.1 先补失败测试，证明画面生成不得创建新的逐分镜视频草稿或任务。
- [ ] 1.2 补齐现有图片候选、预览、排除、单镜头重生和主图唯一性回归测试。
- [ ] 1.3 为历史视频任务只读查询补测试，禁止确认、重试和继续执行。

## 2. 菜单与后端边界

- [ ] 2.1 新增数据库菜单 migration，将“素材生成”显示名称改为“画面生成”。
- [ ] 2.2 移除或封闭旧视频任务创建、确认和 Worker 领取写路径。
- [ ] 2.3 实现按分镜排序的主画面 manifest 及输入版本摘要。
- [ ] 2.4 实现缺失、归档、失败主图和计划后输入变化的阻断响应。

## 3. 页面与原型

- [ ] 3.1 更新 Pencil 画面生成原型，移除逐分镜视频区域并取得用户明确确认。
- [ ] 3.2 更新页面名称、说明和操作，只保留图片候选工作流。
- [ ] 3.3 实现缺图提示和进入作品生成的显式入口。
- [ ] 3.4 更新前端单测与桌面端 E2E，确认菜单来自数据库配置。

## 4. 集成验证

- [ ] 4.1 与作品生成 change 对接主画面 manifest，不双写旧视频任务。
- [ ] 4.2 核验历史记录、图片素材和候选数据没有被迁移或删除。
- [ ] 4.3 运行数据库、后端、Worker、前端、OpenSpec 和 diff 验证后申请归档。
