## 1. OpenSpec、依赖与合同

- [x] 1.0 在实施前反向追溯 `plan-phase-one-drama-mvp-a` 总体任务 `3.1` 及共享任务 `5.1`、`5.3`、`5.4`、`5.5`，并以代码/schema/测试核验 scenes、workflows、catalog 是直接实施依赖。完整非目标是拥有 Provider/Profile/Model catalog CRUD、credential service、WorkflowRun/NodeRun/RunEvent/ProviderCall 状态或事件历史，创建/执行/推进 WorkflowRun，图片/视频/音频生成、媒体二进制入库、Timeline、自动采纳候选、默认真实 Provider 调用，以及把总体协调 change、未完成依赖或真实 Provider 当作运行时依赖/静默 fallback。
- [x] 1.1 以 DDD/BDD/SDD/TDD 固定精确计数及每集时长 CreativeBrief input snapshot、项目级 StorySpec、每集 ScriptSpec、稳定 Scene/Shot、版本化 ShotSpec、AssetBible project->episode->scene->shot 覆盖、完整候选图的一次 TextReviewBatch 审核/stale 传播/批次接受与含 candidate/source hashes、payload hash、expected revisions/correlation 的 owner typed batch command/idempotent ack 完成前付费暂停、审计与 canonical `schema_version` 到 HTTP `schemaVersion` 的同值映射及冲突无写入合同。
- [x] 1.1b 将叙事对象实际引用的初始 Character/Look/Location/SceneVisual/Prop/VisualStyle entry specs 纳入同一候选图/TextReviewBatch；先写 missing/stale/foreign/hash mismatch 与 AssetBible ack 失败测试，再实现 typed handoff，禁止 text owner 写 AssetBible current/override/task。
- [x] 1.1b 固定 MVP-A 文本输出 allowlist 为 StorySpec/ScriptSpec/Episode/Scene/Shot/ShotSpec；添加 `materialType=novel` 仅作为来源、`novel_body|chapter|chapter_draft`/语义等价输出在 TextModelPort 或持久化前返回 `unsupported_output_type` 且零 candidate/ProviderCall/RunEvent/媒体副作用的合同与失败测试。
- [x] 1.1a 定义 `creationMode=original|adaptation`：original 以 CreativeBrief 继续且无 SourceMaterial；adaptation 固定 `materialType=novel|synopsis|existing_script`、`inputMode=inline_text|uploaded_file`、immutable SourceMaterial/SourceMaterialVersion、parse/validation、contentHash 和 binding snapshot。uploaded_file 才有 verified AssetVersion 引用，inline_text 不创建 storage session/StoredObject/AssetVersion；初始 command 只接受 project scope，输出 scope 显式包含 Episode/Scene/Shot。
- [x] 1.2 将 AgentScope 2.x 作为 Agent Worker 独立 runtime dependency 写入依赖清单与 lock；按来源类型审计并固定 `novel-writing`/`drama-skills`：Git Skill 使用 commit/digest，公开 Markdown Skill 使用 archive URL/获取时间/digest/license status。保存 manifest、network/subprocess/file/secret access matrix 与静态证据，编写来源漂移、Registry 启动只读 index/approved metadata、路由后才读取 `SKILL.md`/references、未授权访问、未审计和第三方脚本不得执行的失败测试；覆盖八项 registry candidate、默认仅 approved two-skill binding、其余 `pending_provenance`/disabled 仅在 `allowedSkills`/`requiredCapabilities`/`selectionMode=fixed|inherit` 后按需读取。
- [x] 1.3 增加架构依赖测试，确认文本切片不拥有 catalog CRUD、图片/视频、素材二进制、Timeline、WorkflowRun，也不直接写 Project/Episode/Scene/Shot repository/table；这些 aggregate 只能通过 owner typed batch command/ack 交接。
- [x] 1.4 定义 SkillRouteDecision/SkillRouteSelection Schema 和状态：候选 ID/digest、过滤/淘汰/排序原因、lexical/semantic score/source、confidence/tie、router policy/version、project/node/launch/fingerprint、expected revisions 与 actor；覆盖 deterministic unique、并列/低置信/semantic unavailable、人工选择和最终 frozen SkillRevision。

## 2. 领域、应用与持久化

- [x] 2.1 先写 candidate/batch 状态、provisional 来源链（同 Run/项目、Schema-valid、hash/revision/scope 匹配或 accepted owner fact）、逐对象 Schema、上游修改导致下游 stale、跨项目、批次完整性、一次 immutable batch、CAS revision、owner handoff/ack 幂等和 ack 不完整时媒体门关闭的 domain/application 失败测试，再实现 entities、Commands、Queries、Repository/UoW/Outbox ports。
- [x] 2.2 先写 TextModelPort/AgentScope adapter、Mock/真实 opt-in、结构化解析和失败审计测试，再实现 composition。
- [x] 2.2a 先写 fake transport contract tests，再实现 `OpenAICompatibleTextModelAdapter`：profile-derived base URL/endpoint、adapter-bound Bearer/API-key auth、`GET /v1/models` 只读 candidate diff（手工 model 保留、显式 accept 才同步 catalog）、受 capability snapshot 约束的 structured request、request-id/usage/JSON payload 解析；仅对 transport/429/5xx/timeout 的 bounded retry，unknown 先 reconciliation，4xx/malformed/schema failure 不重试；Codex relay 默认、DeepSeek opt-in，缺 profile/credential/master-key 返回原始诊断且不 fallback，日志不保存 secret。
- [x] 2.3 新增并验证可逆 Alembic migration，建立 candidate、text_review_batch/member、audit/confirmation/outbox、owner-handoff/ack 表及项目、状态、版本、hash 和外键约束；migration 不创建或修改 Project/Episode/Scene/Shot owner 表。
- [x] 2.3a 为 creation/source revision、parse/validation、binding snapshot 和 uploaded_file verified AssetVersion reference 增加 additive persistence/contract fixtures；original 无 source，inline_text 无 storage session/StoredObject/AssetVersion；不得保存或覆盖媒体 bytes/AssetVersion，并以 owner-defined orchestration handoff 避免文本 owner 直接写 Project/Episode/Scene/Shot aggregate。
- [x] 2.4 先写 route candidate drift、non-candidate、disabled/unapproved、重复/冲突 selection 和刷新零自动选择测试，再实现 decision/selection Repository/UoW、`ResolveSkillRouteDecision`、audit/Outbox 与 workflows frozen-snapshot handoff；选择前零 Run/NodeRun/TextModel/Provider。

## 3. 接口与验证

- [x] 3.1 先写 HTTP/BDD/contract 失败测试，再实现 generate/read/list、batch read/confirm/reject API、camelCase DTO、审计查询和稳定错误映射；HTTP `schemaVersion` 只映射 canonical `schema_version`，batch 确认 API 只追加 accepted handoff，不直接写 Project/Episode/Scene/Shot，也不创建/推进 WorkflowRun 或 ProviderCall 事件。
- [x] 3.2 覆盖 JSON Schema 无效、Skill drift、network/subprocess/file/secret 未授权访问和脚本执行拒绝、provisional 来源跨 Run/项目、上游修改后的 stale/partial batch、重复或过期 batch 决定、直接依赖未冻结、直接写 Project/Episode/Scene/Shot 或其他运行领域所有权泄漏、owner ack 缺失/失败、真实 provider 未配置/失败、usage/cost 脱敏、超过诊断窗口与不同 hold/restart/reconcile 后 Agent Worker cleanup 不删除/覆盖/静默压缩关联 `RunEvent`、`CapabilitySnapshot`、脱敏 `ProviderCall` 摘要或仍被引用 `AssetVersion`，以及版本缺失、冲突或双独立赋值时无 candidate/batch/audit/confirmation/handoff/ack/领域/Outbox 写入。
- [x] 3.2a 覆盖 original 无 SourceMaterial 仍可继续、adaptation 的 novel/synopsis/existing_script 与 inline_text/uploaded_file 成功/失败、invalid enum/scope/revision、foreign/stale/unverified AssetVersion、source-bound Run recovery/reconciliation、inline 零 storage mutation、original/inline upload intent rejection 和 no-reupload/no-duplicate-paid-operation 的 HTTP/BDD/TDD 场景。
- [x] 3.2b 覆盖 Skill route list/read/resolve HTTP、候选/原因/歧义 projection、decision/launch 409、设置页启停不替代 route selection、默认 Workflow 仍仅绑定 approved novel-writing/drama-skills 和所有失败零模型/Provider 副作用。
- [x] 3.3 运行定向 domain/application（candidate/确认/审计）、adapter/worker（AgentScope lock、TextModelPort、Mock/opt-in）、HTTP/contract/BDD（scope、确认、运行边界、`schema_version`/`schemaVersion` 同值映射及冲突无写入）、migration 测试及 SQLite/PostgreSQL cycle。
- [x] 3.4 运行 `openspec instructions apply --change "integrate-agentscope-text-skills" --json`、`openspec status --change "integrate-agentscope-text-skills" --json`、`openspec validate "integrate-agentscope-text-skills" --strict --json`、`openspec validate --changes --strict --json`、`pnpm run check` 与 `git diff --check -- openspec/changes/integrate-agentscope-text-skills`。

## 4. 默认 Workflow Text Port

- [x] 4.1 定义 `text.generate` CreativeBrief input、完整 TextReviewBatch output、accepted immutable handoff、Project/Episode/Scene/Shot owner typed batch command/ack 与最终 storyboard/reference handoff 的 contract/schema 和 port fixtures。
- [x] 4.2 添加 partial/stale/foreign/unaccepted batch 的拒绝及零媒体副作用测试，确认默认 E2E 仅使用 `Mock Provider +` 显式 Local test/offline profile（adapter identity=`local_workspace`）。
- [x] 4.3 实现前先定义/测试 `regenerateStaleClosure` 或 `regenerateTextCandidate`：source ids/hashes、run/brief/batch/expected revisions、逐对象 schema/count/scope/hash 校验；成功才产生 successor batch，old immutable，CAS 全有或全无。
