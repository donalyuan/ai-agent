# script-to-asset-generation Tasks

## 1. OpenSpec 和 Pencil 原型

- [x] 1.1 补齐 proposal、design、spec 和 tasks。
- [x] 1.2 运行 OpenSpec 校验。
- [x] 1.3 更新 `docs/prototypes/video-agent/video-agent.pen`，覆盖脚本详情素材候选交互。
- [x] 1.4 通过 Pencil MCP 读取或截图验证原型。
- [x] 1.5 等待用户明确确认原型。

## 2. 后端 schema 和仓储

- [x] 2.1 新增 `asset_generation_tasks` 和 `scene_asset_candidates` migration，不修改已应用旧 migration。
- [x] 2.2 新增素材候选和生成任务 domain model。
- [x] 2.3 新增 repository 和契约测试，覆盖候选唯一选中、归档素材不可选、任务状态流转。

## 3. 后端 API 和业务服务

- [x] 3.1 新增生成计划 API，覆盖 48 张上限和供应商校验。
- [x] 3.2 新增任务创建 API，创建图片自动任务和视频草稿任务。
- [x] 3.3 新增候选查询、选择、排除和单分镜重生 API。
- [x] 3.4 新增 AI 视频二次确认 API。
- [x] 3.5 补齐路由测试和错误映射。

## 4. Worker、供应商和存储

- [x] 4.1 在 `services/video-worker` 新增图片生成任务处理流程。
- [x] 4.2 新增 `gpt-image-2` provider adapter 和 fake provider 测试。
- [x] 4.3 新增即梦 provider adapter，按官方文档/SDK 或实测确认字段。
- [x] 4.4 新增本地素材存储写入和 `/assets/...` 稳定访问策略。
- [x] 4.5 覆盖临时错误重试 1 次、部分成功、下载失败不入库。

## 5. 前端 API、页面和状态编排

- [x] 5.1 新增素材候选 API wrapper 和测试。
- [x] 5.2 更新脚本详情页，采用左分镜、中候选、右设置布局。
- [x] 5.3 接入供应商选择、候选数量、参考图开关、生成计划和任务状态。
- [x] 5.4 接入选择、排除、单分镜重生和视频二次确认交互。

## 6. E2E 与收尾验证

- [x] 6.1 补齐脚本详情到素材候选选择 E2E。
- [x] 6.2 运行后端、worker、前端、OpenSpec 和 diff 验证。
- [x] 6.3 执行 `openspec instructions apply --change "script-to-asset-generation" --json` 并确认状态。

## 7. Review 阻塞修复

- [x] 7.1 API 暴露 `/assets/...` 本地素材静态访问前缀，并由测试覆盖。
- [x] 7.2 Worker 接通待处理图片任务消费、素材入库、候选回写和任务状态回写。
- [x] 7.3 批量生成任务改为幂等，重复点击不得重复创建旧素材候选、视频草稿任务或同配置图片任务。
- [x] 7.4 新增脚本素材生成任务列表读取 API，前端页面重开后恢复真实任务状态。
- [x] 7.5 供应商启用配置由后端计划返回，前端只展示已启用供应商。
- [x] 7.6 Worker 状态协议与数据库约束保持一致，部分成功使用 `completed + result.partial=true` 表示。
- [x] 7.7 OpenAI 图片请求按官方 Image API 参数实现，不发送不支持的 JSON 字段。

## 8. 用户截图反馈修正

- [x] 8.1 新增 `素材管理 / 素材生成` 独立二级菜单 migration，不修改已应用旧 migration。
- [x] 8.2 将素材候选生成从 `脚本创作 / 脚本生成` 页面拆出，只在 `素材生成` 页面展示。
- [x] 8.3 去掉 `Topic Source`、独立 `Agent`、`素材 Agent` 等说明性小标题，并把来源选题类型显示为中文。
- [x] 8.4 对运行库执行 SQLx migration，并核验 `_sqlx_migrations` 和实际菜单数据。
- [x] 8.5 补齐并运行后端菜单/迁移测试、前端单测/E2E、OpenSpec 和 diff 校验。

## 9. 运行态回归修复

- [x] 9.1 重启 API 和 Worker，使当前运行进程加载素材生成路由与任务消费逻辑。
- [x] 9.2 验证素材生成路由不再返回路由级 `404`，且不触发真实供应商生成费用。

## 10. 单镜头重生费用防重

- [x] 10.1 补充单镜头幂等协议、在途任务唯一约束和费用安全测试规格。
- [x] 10.2 新增数据库 migration，约束同一分镜最多一个在途图片任务。
- [x] 10.3 后端按 `Idempotency-Key` 和同分镜在途任务原子创建或复用任务。
- [x] 10.4 前端为单镜头重生增加同步请求锁并发送 UUID 格式幂等键。
- [x] 10.5 覆盖相同 key、不同 key 并发、终态后新建、前端快速连点和 Worker 单任务消费测试。
- [x] 10.6 运行后端、前端、Worker、OpenSpec 和 diff 验证。

## 11. 图片任务运行态可见性修复

- [x] 11.1 根据运行库、API、前端过滤和 Worker 配置证据定位根因并更新规格。
- [x] 11.2 补充图片任务可见、在途轮询、素材 URL 解析和 Worker 失败终态测试。
- [x] 11.3 前端展示图片任务并在在途期间自动刷新任务与候选素材。
- [x] 11.4 修复 Worker 显式启用、环境加载、健康可观测和领取后异常终态回写。
- [x] 11.5 验证 Compose 配置但不启动 Worker，确认现有待处理任务和费用状态不变。
- [x] 11.6 运行前端、Worker、OpenSpec 和 diff 全量验证。

## 12. 图片供应商 403 运行态修复

- [x] 12.1 关闭 Worker 并通过非计费 `/v1/models` 探测确认 `gpt-image-2` 可用及图片 API 根路径。
- [x] 12.2 补充 `/responses -> /v1`、兼容 User-Agent、HTTP 错误摘要和永久错误批次熔断测试。
- [x] 12.3 修复 OpenAI 图片端点推导和请求身份，并在首个永久错误后停止剩余供应商调用。
- [x] 12.4 重建 Worker，清理本次失败占位候选并对用户确认的 18 张任务执行一次受控重试。
- [ ] 12.5 验证任务终态、素材落盘与 API 访问、前端候选展示、费用范围和全量测试。（受控重试已确认失败终态、错误展示和永久错误熔断；成功素材验证阻塞于上游 group 未启用图片生成，Worker 已关闭。）

## 13. 失败任务清理与镜头内容展示

- [x] 13.1 在现有 change 补充软删除审计、仅失败任务可清理、候选过滤和镜头内容展示规格。
- [x] 13.2 更新 `docs/prototypes/video-agent/video-agent.pen`，提供主界面与清理确认弹窗，并通过截图和布局检查。
- [x] 13.3 等待用户明确确认 Pencil 原型后再进入正式开发。
- [x] 13.4 先补数据库、仓储和路由失败测试，再新增 `dismissed_at` migration 与幂等清理 API。
- [x] 13.5 先补前端 API 和页面失败测试，再实现清理确认、请求锁、任务/失败候选同步刷新和错误保留。
- [x] 13.6 先补页面测试，再在候选素材上方实现当前镜头“旁白 / 画面”双栏内容区，并保持左侧分镜列表紧凑。
- [x] 13.7 运行后端、前端、E2E、OpenSpec、Pencil 布局和 diff 全量验证，确认清理路径不触发 Worker 或供应商。
- [x] 13.8 复现运行 API 缺少清理路由和 migration 的问题，先补启动迁移失败测试，再让 API 启动连接自动应用待执行 SQLx migration。
